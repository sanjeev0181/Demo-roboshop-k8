apiVersion: v1
kind: Pod
metadata:
  name: nginx-ns
  labels:
    name: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
      - configMapRef:
            name: configure-map
            key: Name